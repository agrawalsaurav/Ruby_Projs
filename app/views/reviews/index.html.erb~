<h1>Movies</h1>
<%= include_gon %>

<h1>Reviews</h1>

<table>
	<thead>
    <tr>
	    <th width="210">Email</th>
	    <th width="200">Date</th>
	    <th width="100">Rating</th>

      <th width="300">Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @reviews.each do |review| %>
      <tr>
	<td align="center"><%= review.email %></td>
	      <td align="center"><%= review.created_at %></td>
	      <td align="center"><%= review.rating '/10' %></td>
        <td align="center"><%= review.comment %></td>
        <td width="100"><%= link_to 'Show', review %></td>
        <td width="100"><%= link_to 'Edit', edit_review_path(review) %></td>
       
</tr>
    <% end %>

  </tbody>
</table>

<br>

<%= link_to 'New Review', new_review_path %>
